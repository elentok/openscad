// ------------------------------------------------------------
// Mirrors while copying the object
// ------------------------------------------------------------
module mirror_copy(vec) {
  children();
  mirror(vec) children();
}
